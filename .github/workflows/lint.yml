name: Lint

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12" 
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r producer/requirements.txt && pip install -r requirements_dev.txt
    - name: Check formatting
      run: black . -t py312 -l 120 --check
    - name: Check imports
      run: isort . --check
    - name: Check typing
      run: mypy .
    - name: Check style
      run: flake8 .
